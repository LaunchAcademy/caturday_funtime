<h2> <%= @category.tag %></h2>

<ul>
  <% @cats.each do |cat| %>
    <li>
      <p><%= link_to image_tag(cat.url), cat %></p>
      <p><strong><%= link_to cat.name, cat %></strong><br>
      posted by <br> <%= cat.user.email %></p>
    </li>
  <% end %>
</ul>

<%= link_to "Back to all cats", cats_path %> |
<%= link_to "View all categories", categories_path %>
